# GREET Dataset Archive

This repository contains exported GREET datasets for version control.

## Structure

- `datasets/` - Contains exported datasets in JSON format
- Each dataset folder contains chunked JSON files organized by entity type

## Usage

Use the PyGREET desktop app to import/export datasets from this archive.
